<!DOCTYPE html>
<html>
<head>
  <link href='http://fonts.googleapis.com/css?family=Patrick+Hand+SC' rel='stylesheet' type='text/css'>

  <title>nom nom</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

 </head>
<body>

<div class="navbar">
    <div class="navbar-inner">
       <% if current_user.present? %>
          <a class="brand" <%= link_to "nom nom", home_url %>
        <% else %>
          <a class="brand" <%= link_to "nom nom", login_url %>
        <% end %>
    <ul class="nav">
    <li><a <%= link_to "Where do you wanna eat?", restaurants_url %></a></li>
    <li><a <%= link_to "What do you wanna eat?", dishes_url %></a></li>
    <li><a <%= link_to "Write a review", new_meal_url %></a></li>
    <% if current_user.present? %>
             <li><%= link_to "My Profile", user_url(current_user.id) %></li>
              <li><%= link_to "Logout", logout_url %></li>
            <% else %>
            <li><%= link_to "Sign Up", signup_url %></li>
            <li><%= link_to "Login", login_url %></li>
            <% end %>
  </ul>
  </div>
</div>
<h3><div class="item", align="center">
  <%= notice %>
</div></h3>
<%= yield %>
</body>
</html>


